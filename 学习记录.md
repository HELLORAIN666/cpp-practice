# 算法组第一次作业 · 学习记录

| 项目 | 内容 |
|---|---|
| 姓名 | 李浩宇 |
| 专业 | 机械电子工程 |
| 方向 | 算法组 |
| 日期 | 2026 年 9 月 |
| 环境 | Windows + MinGW-w64 GCC 15.3.0 / VS Code / C++17 |

---

## 一、作业概览

本次作业共 9 题（题面标注 T1–T10，其中 T6 在题目文档中缺失），内容从基础输入输出一路覆盖到类的设计。

| 题号 | 题目 | 核心知识点 |
|---|---|---|
| T1 | 小玉买文具 | 数据类型、单位统一、整数除法 |
| T2 | 数组求和 | 数组、循环、累加器 |
| T3 | 简易计算器 | switch 语句、break 穿透 |
| T4 | 最大值与最小值 | 打擂台法、初始值陷阱 |
| T5 | 指针交换变量 | 指针、地址传递、函数参数 |
| T7 | 乘积最大 | 循环与条件判断、均值思想 |
| T8 | 成绩排序 | 结构体、sort 自定义比较、函数指针 |
| T9 | 最接近的三数之和 | 排序 + 双指针、复杂度优化 |
| T10 | 图书管理系统 | 类的设计、封装、构造析构、运算符重载 |

---

## 二、逐题记录

### T1 小玉买文具

**思路**：把钱和笔价都换算成「角」，再用整数除法算能买几支。

- 笔价：1 元 9 角 = **19 角**
- 小玉的钱：a 元 b 角 = **a × 10 + b 角**
- 最多能买：总角数 / 19

**为什么不用浮点数做**：0.1 这类小数在 `double` 里是近似存储，根本存不准。涉及钱的计算一律换算成最小单位、用整数算，这是行业惯例。

**样例验证**：输入 `10 3` → `(10×10+3) / 19 = 103 / 19 = 5`。整数除法自动向下取整，语义上正好就是「最多能买几支」。 ✓

---

### T2 数组求和

**思路**：开一个数组把数存下来，再用一个累加器 `sum` 循环累加。

- `sum` 用 `long long` 而不是 `int`：n 个数相加可能超出 int 范围，用 64 位更保险。
- 本题也可以用「边读边加」的写法省掉数组，但为了练习数组，采用了「先存、再遍历」的方式。

---

### T3 简易计算器

**思路**：输入格式是 `a op b`（中间有空格），`cin >> a >> op >> b` 会自动按空格切开。用 `switch` 根据 `op` 分发到四个分支。

**除法**：题目要求整数除法（向零取整），C++ 中 `int` 相除天然满足这个规则。除数为 0 时要特判，输出 `Error!`。

> **⚠️ 自查发现的两个问题（已修正）**
>
> 1. **`case '*'` 后面漏写了 `break`** —— switch 一旦进入某个 case，会一路「滑」到后面的 case（穿透）。实测输入 `3*4`，程序输出了 `12` 和 `0` 两行：`12` 是乘法结果，之后又掉进除法分支算了 `3/4 = 0`。
> 2. **输出字符串多打了感叹号** —— 题面样例要求输出 `Error!`，我写成了 `Error!!!`。OJ 是逐字符比对的，多一个标点就是 0 分。
>
> **教训**：`break` 是每个 case 门口的「止步牌」，忘了写就会执行别人的代码。

---

### T4 最大值与最小值

**思路**：打擂台法。让数组的第一个元素先当擂主，后面的数依次上来挑战——比我大就换人，比我小也换人。

**关键陷阱**：初始值不能随便设 0！如果所有的数都是负数（比如 `-5 -3 -1`），拿 0 当初始最大值，结果会输出 0，但 0 根本不在数组里。正确做法是**用数组的第一个元素当初始值**（擂主必须从参赛选手里选）。另一种写法是 `maxVal` 初始化为 `INT_MIN`、`minVal` 初始化为 `INT_MAX`（需要 `<climits>`）。

> **⚠️ 自查发现的问题（已修正）**
>
> 输出写成了 `cout << Min << "" << Max;` —— `""` 是**空字符串**，不是空格，什么都不会输出；结尾还漏了 `endl`。
> 实测输入 `1 5 -2`，输出是 `-25`（最小值 -2 和最大值 5 直接粘成了一个字符串）。正确写法是 `cout << minVal << " " << maxVal << endl;`

---

### T5 指针交换变量

**思路**：写 `void swap(int *x, int *y)`，用指针接收实参的地址，函数内通过 `*x` 直接改写主函数里的那个变量。

```cpp
void swap(int *x, int *y)
{
    int temp = *x;   // 取出 x 指向的那个值
    *x = *y;         // 把 y 指向的值写进 x 指向的房间
    *y = temp;       // 再把原来的值写进 y 指向的房间
}

// 调用：& 是「取地址」，把 a 的门牌号交给函数
swap(&a, &b);
```

为什么普通传值不生效，见**第四节**的思考题回答。

---

### T7 乘积最大

**思路**：把 N 尽量「平均分」给 M 个正整数，乘积最大。

**为什么平均分是最大的（本题灵魂）**：

假设有两个数 a 和 b，且 b - a ≥ 2（差距至少 2）。现在从 b 身上拿 1 给 a：

```
新乘积 = (a+1)(b-1) = ab - a + b - 1 = ab + (b - a - 1)
```

因为 b - a ≥ 2，所以 (b - a - 1) ≥ 1 > 0，即**新乘积一定大于原乘积**。

结论：只要还有两个数的差距 ≥ 2，就把它们拉平一点，乘积就会变大 → 一直拉到所有数只差 0 或 1，就是最大乘积。

**生活比喻**：给你 10 米篱笆围一个矩形菜地，围成正方形（边长接近）面积最大；拉成又细又长的条，面积就变小了。道理完全一样。

**实现**：

- `base = N / M` —— 每个人先分到的「保底」
- `rest = N % M` —— 剩下 rest 个没分完，给最后 rest 个人各多 1
- 于是前 (M - rest) 个人是 base，后 rest 个人是 base + 1，**天然升序**，不用额外排序

**样例验证**：输入 `6 3` → `base = 2, rest = 0` → 输出 `2 2 2` ✓

---

### T8 成绩排序

**思路**：

1. 一个学生有「名字」和「分数」两个类型不同的属性 → 用 `struct` 打包成一个整体（像一个快递盒，把零散的东西装成一个包裹，方便整体搬运和排序）。
2. 排序规则是复合的（先比分数，分数相同再比名字）→ 写一个比较函数告诉 `sort` 什么叫「前一个更靠前」。
3. 用 `new Student[n]` 动态分配数组，用完必须 `delete[]` 归还，否则内存泄漏。

**比较函数**：

```cpp
// 分数高的在前；分数相同，名字字典序小的在前
bool cmpByScore(const Student &a, const Student &b)
{
    if (a.score != b.score)
    {
        return a.score > b.score;   // 降序
    }
    return a.name < b.name;         // string 的 < 就是字典序比较
}
```

参数用 `const 引用` 传，避免每次比较都拷贝一份结构体。

**提高部分（函数指针 + 枚举）**：把「比较规则」本身也做成一份可以插拔的数据。

```cpp
typedef bool (*CompareFunc)(const Student &, const Student &);

enum SortType
{
    BY_SCORE = 0,
    BY_NAME,
    TYPE_COUNT
};

// 函数指针表：下标就是枚举值
CompareFunc Data_Process[TYPE_COUNT] = {cmpByScore, cmpByName};

// 注册函数：把某个处理函数挂到某个枚举位上
void Function_Register(CompareFunc f, SortType type)
{
    Data_Process[type] = f;
}
```

这样做的好处：以后要加一种排序方式，只需要写一个新函数再注册进去，**不用改排序的主逻辑**。

---

### T9 最接近的三数之和

**暴力法**：三重循环 O(n³)，n 一大就爆炸。

**优化思路（本题灵魂）**：

1. 先排序。
2. 固定第一个数 `nums[i]`，问题就变成了「在 i 后面的区间里找两个数，让它们的和最接近 `target - nums[i]`」—— 这就是经典的「两数之和最接近」，用双指针可以 O(n) 解决。
3. 外层 i 循环 × 内层双指针 = **O(n²)**，比暴力快一个数量级。

**双指针为什么能工作（一定要想明白）**：

数组已经升序，左指针 `L` 在头，右指针 `R` 在尾。看当前和 `cur = nums[i] + nums[L] + nums[R]`：

- `cur` 太小 → 想变大，只能把 `L` 往右挪（换一个更大的数进来）
- `cur` 太大 → 想变小，只能把 `R` 往左挪（换一个更小的数进来）

因为数组有序，指针移动的效果是**单调可预测**的，所以永远不会漏掉最优解。两个指针加起来总共只走 n 步。

**比喻**：你要在一排从矮到高排好队的人里，找两个人的身高之和最接近 3 米 5。你一手指向最矮的、一手指向最高的；加起来太矮，就把左手往右移（换高的）；太高，就把右手往左移（换矮的）。两只手相向而行，扫一遍就有答案。

**输入处理**：第一行是 target，之后一直读到 EOF（用 `while (cin >> x)`，配合 `vector` 自动扩容，不用事先知道有多少个数）。

> **小提示**：在终端手动测试时，输完数字要按 `Ctrl+Z` 再回车，程序才会知道输入结束了。

---

### T10 图书管理系统

**设计思路：把「现实世界」翻译成「代码世界」**

| 现实 | 代码 |
|---|---|
| 一本书 | `Book` 类的一个对象（成员变量 = 属性，成员函数 = 行为） |
| 图书馆 | `Library` 类的一个对象（持有 `vector<Book>`，提供增删改查） |

**为什么要分两个类（单一职责原则）**：

- `Book` 只管「我自己是什么」：我的书名、我的库存够不够。
- `Library` 只管「怎么管这一堆书」：怎么找、怎么排序、怎么增删。
- 如果全塞进一个 `main` 函数里，代码马上变成一坨谁也不敢碰的面条。

**封装（encapsulation）的意义**：

成员变量设成 `private`，外部不许直接做 `stock = -100` 这种脏操作，只能通过 `borrowOne()` 这种「正规窗口」办事，窗口里能做合法性检查。

> **比喻：银行柜台。** 你不能自己进金库拿钱，只能在窗口办业务，柜员会查你的身份证。

**运算符重载**：

- `operator==`：两本书「相等」定义为编号相同。
- `operator<`：按编号升序。这样 `sort(books.begin(), books.end())` 直接就能用，**不用再写一个比较函数**。

```cpp
bool operator<(const Book &other) const
{
    return this->id < other.id;
}
```

**构造 / 析构函数**：

对象「出生」时必须被正确初始化，否则成员变量是随机的垃圾值。构造函数就是「出生证明 + 初始配置」，在对象创建的瞬间自动调用一次；析构函数在对象销毁时调用。

特别提醒：**只打印、不赋值的成员变量必须给默认值**（比如默认构造里把 `title` 设成 `""`），否则读出来是垃圾值。

**用状态码区分两种失败原因**：

`borrowBook(id)` 返回 `0 = 成功 / 1 = 没这本书 / 2 = 没库存`，让调用方（`main`）决定该打印哪一句提示。这样业务逻辑和输出解耦。

**输出格式**（严格对齐题目）：

```
ID:1001, Title:C++_Primer, Author:Stanley, Price:89.50, Stock:5
```

价格用 `fixed` + `setprecision(2)` 保留两位小数。

---

## 三、血泪教训总结

### 1. switch 穿透

`switch` 进入某个 case 后，会一路往下执行到 `break` 或 switch 结束。**每个 case 结尾都要写 `break`**，除非是故意利用穿透（比如多个 case 共用一个分支）。

### 2. 输出格式里「看不见的字符」

最容易丢的三样东西：

| 丢的东西 | 我的错误写法 | 后果 |
|---|---|---|
| 数字之间的空格 | `Min << "" << Max` | 两个数粘成 `-25` |
| 行末换行 | 漏写 `endl` | 和下一行输出挤在一起 |
| 字符串里的标点 | `"Error!!!"` | 应为 `"Error!"`，判错 |

**这类错误编译器一声不吭，但结果就是错的。** 唯一的办法是造几组数据实跑一遍，用眼睛看输出。

### 3. 打擂台的初始值

求最值时，初始值必须从数组里选（第一个元素），不能凭空捏一个 0。否则数组全是负数时会输出一个不在数组里的 0。

### 4. 整数除法 vs 浮点

- `int / int` 自动向零取整，语义上正好是「最多能买几个」。
- 涉及钱不要用 `double`，换算成最小单位（角 / 分）用整数算。

### 5. 编译器不报错 ≠ 代码正确

本次自查出的两个问题（T3 漏 `break`、T4 空字符串）编译器**没有任何警告**。所以：**写完代码必须自己测，不能靠眼睛检查。**

---

## 四、思考题（T5）：传值形式的 swap 为什么不能交换？

把变量想象成一间**宿舍**，变量名是门牌号，里面住着值。

**传值 `swap(int x, int y)`**：
函数新开了两间「临时房」x 和 y，把 a、b 房间里的东西**复印**了一份搬过去。你在函数里把 x、y 房间互换了 —— 但 a、b 原来的房间纹丝未动。函数一结束，临时房被销毁，什么都没发生。

**传址 `swap(int *x, int *y)`**：
这次传过去的不是「房间里的东西」，而是**门牌号（地址）**。函数拿着你家的地址直接上门，把里面的东西换了 —— 所以 a、b 真的变了。

> **一句话**：传值 = 给你复印件让你改；传址 = 给你钥匙让你进屋改。

---

## 五、测试验证记录

作业完成后，用题面给出的**全部样例**逐条实跑，结果如下。

| 题 | 测试输入 | 期望输出 | 实际输出 | 结果 |
|---|---|---|---|---|
| T1 | `10 3` | `5` | `5` | ✓ |
| T2 | `5` / `1 2 3 4 5` | `15` | `15` | ✓ |
| T3 | `10 + 3` | `13` | `13` | ✓ |
| T3 | `10 - 3` | `7` | `7` | ✓ |
| T3 | `10 * 3` | `30` | `30` | ✓ |
| T3 | `10 / 3` | `3` | `3` | ✓ |
| T3 | `10 / 0` | `Error!` | `Error!` | ✓ |
| T3 | `5 - 8` | `-3` | `-3` | ✓ |
| T4 | `5` / `3 1 4 1 5` | `1 5` | `1 5` | ✓ |
| T5 | `3 5` | `5 3` | `5 3` | ✓ |
| T7 | `6 3` | `2 2 2` | `2 2 2` | ✓ |
| T8 | 题面样例 #1 | Joey 92 / Hanmeimei 90 / Kitty 80 / Tim 28 | 一致 | ✓ |
| T9 | `1` / `-1 2 1 -4` | `2` | `2` | ✓ |
| T9 | `1` / `0 0 0` | `0` | `0` | ✓ |
| T10 | 题面样例 #1 | 见题面 | 一致 | ✓ |
| T10 | 题面样例 #2 | 见题面 | 一致 | ✓ |

**结论**：全部样例通过，输出与题面逐字符一致。

编译命令（与 `.vscode/tasks.json` 一致）：

```bash
g++ -g -Wall -fexec-charset=GBK -static -std=c++17 tX.cpp -o output/tX.exe
```

---

## 六、待确认事项（题面疑似笔误，非代码问题）

1. **T10 样例 #1**：题面写 `n = 10`，但实际列出了 **11 条操作**。若按 n = 10 执行，最后一条 `show` 会被丢弃，输出会少两行。改成 n = 11 后输出与题面期望**完全一致**。判断为题面笔误，代码按 `for (k = 0; k < n; k++)` 实现是正确的。
2. **T10 样例 #2**：`add Alice 1001 29.99 0` 只有 4 个参数，而按格式 add 需要 5 个（`title author id price stock`），少了一个 author。判断为题面笔误。
3. **T9 输入**未给出数组长度，按「读到 EOF 为止」实现。手动测试时输完数字需按 `Ctrl+Z` 再回车。

---

## 七、自我总结

**这次走通的部分**

- 类与对象：`Book` + `Library` 两个类的拆分，`private` 成员 + 对外接口，构造 / 析构 / 运算符重载都用上了。
- 算法：T9 的「排序 + 双指针」是第一次自己独立想明白的优化思路，从 O(n³) 降到 O(n²)。
- T8 的提高部分：理解了函数指针为什么能当「可插拔的零件」用。

**这次暴露的问题**

- **细节把控不够**。T3 漏 `break`、T4 用空字符串代替空格，都是「会做但写错」的类型。这类错最亏：思路全对，机器不认。
- **没有自测的习惯**。第一版写完就直接放着了，没有造数据跑一遍（后来在自查中补上了，见第五节）。

**下一步要做的事**

1. 每道题写完，**至少造 2 组数据**（一组样例、一组边界）实跑，肉眼核对输出。
2. 特别留意字符串和格式 —— 对照题面样例逐字符比一遍。
3. 继续练指针和结构体，为后面的数据结构打基础。
